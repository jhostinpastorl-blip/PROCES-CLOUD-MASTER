import Link from "next/link";
import { ProcesaLogo } from "./procesa-logo";
import { ThemeToggle } from "./theme-toggle";
import { Icon } from "./icon";

const legacySteps = ["Plan", "Empresa", "Módulos", "Sucursal", "Listo"];

export function OnboardingShell({step,title,subtitle,children,steps=legacySteps,backHref,context}:{step:number;title:string;subtitle:string;children:React.ReactNode;steps?:readonly string[];backHref?:string;context?:string}){return <main className="onboarding-premium"><header className="onboarding-header"><div className="onboarding-brand"><ProcesaLogo/><span>Configuración inicial</span></div><ThemeToggle showLabel={false}/></header><div className="onboarding-body"><aside className="onboarding-progress"><span className="section-kicker">ACTIVACIÓN PROCESA</span><h1>Configura la base de tu operación.</h1><p>Cada paso define el contexto de empresa, sucursal y acceso que utilizará tu equipo.</p>{context&&<div className="onboarding-context"><span>CONTEXTO</span><b>{context}</b></div>}<ol className="stepper">{steps.map((x,i)=><li className={i+1<step?"complete":i+1===step?"active":""} aria-current={i+1===step?"step":undefined} key={x}><i>{i+1<step?<Icon name="check" size={14}/>:i+1}</i><span>{x}</span></li>)}</ol></aside><section className="onboarding-stage"><div className="onboarding-card">{backHref&&<Link className="onboarding-back" href={backHref}><span aria-hidden="true">←</span> Paso anterior</Link>}<span className="step-label">PASO {step} DE {steps.length}</span><h2>{title}</h2><p className="onboarding-subtitle">{subtitle}</p>{children}<small className="onboarding-save-note">Tu avance se guarda al continuar.</small></div></section></div></main>}
