import{createClient}from"@/lib/supabase/server";export async function getCurrentSubscription(companyId:string){const s=await createClient();const{data}=await s.from("subscriptions").select("id,status,starts_at,ends_at,plan_id,plans(code,name,max_users,max_branches)").eq("company_id",companyId).in("status",["trial","active"]).order("created_at",{ascending:false}).limit(1).maybeSingle();return data}export function trialDaysLeft(endsAt:string|null){if(!endsAt)return null;return Math.max(0,Math.ceil((new Date(endsAt).getTime()-Date.now())/86400000))}