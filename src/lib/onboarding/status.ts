import{createClient}from"@/lib/supabase/server";import{getCompanyContexts}from"@/lib/auth/context";
export async function getOnboardingStatus(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)return{step:"plan" as const,companies:[]};const{data:state}=await s.from("onboarding_states").select("current_step,selected_plan_code,company_id,selected_module_codes,completed_at").eq("user_id",user.id).maybeSingle();const companies=await getCompanyContexts();if(state?.completed_at)return{step:"complete" as const,companies,state};if(state)return{step:state.current_step as any,companies,state};if(!companies.length)return{step:"plan" as const,companies};return{step:"complete" as const,companies}}
