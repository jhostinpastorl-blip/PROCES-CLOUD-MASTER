import Link from"next/link";import{signUp}from"./actions";import{AuthShell}from"@/components/ui/auth-shell";import{PremiumField}from"@/components/ui/premium-field";
export default async function Register({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <AuthShell kicker="CREA TU CUENTA" title="Empieza con PROCESA Cloud" subtitle="Cuéntanos sobre tu negocio y te ayudaremos a activar la solución correcta.">{q.error&&<div className="auth-message error" role="alert">{q.error==="validation"?"Revisa los datos e intenta nuevamente.":"No pudimos crear la cuenta. Si ya estás registrado, inicia sesión o recupera tu contraseña."}</div>}<form action={signUp} className="premium-form"><PremiumField label="Nombre completo" name="name" placeholder="Tu nombre"/><PremiumField label="Correo empresarial" name="email" type="email" placeholder="nombre@empresa.com" autoComplete="email"/><PremiumField label="Contraseña" name="password" type="password" placeholder="Mínimo 10 caracteres" autoComplete="new-password"/><label className="terms-line"><input type="checkbox" name="terms" value="yes" required/><span>Acepto los términos y la política de privacidad.</span></label><button className="premium-submit">Crear mi cuenta <span aria-hidden="true">→</span></button></form><p className="auth-switch">¿Ya tienes cuenta? <Link href="/login">Inicia sesión</Link></p></AuthShell>}
