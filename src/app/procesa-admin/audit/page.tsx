import{requirePlatformAdmin}from"@/lib/platform/admin";
export default async function Audit(){const{s}=await requirePlatformAdmin();const{data}=await s.from("platform_audit_logs").select("id,action,entity_type,entity_id,created_at").order("created_at",{ascending:false}).limit(200);return <main className="app-content"><div className="page-head"><div><span className="eyebrow">SEGURIDAD</span><h1>Auditoría de plataforma</h1></div></div><section className="panel"><div className="timeline">{data?.map(x=><div className="timeline-item" key={x.id}><span className="timeline-dot"/><div><b>{x.action}</b><p>{x.entity_type??"platform"} · {x.entity_id??""}</p><small>{new Date(x.created_at).toLocaleString("es-PE")}</small></div></div>)}</div></section></main>}