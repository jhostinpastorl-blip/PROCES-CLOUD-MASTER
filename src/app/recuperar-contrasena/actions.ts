"use server";import{z}from"zod";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";const schema=z.object({email:z.string().email()});export async function requestReset(f:FormData){const p=schema.safeParse(Object.fromEntries(f));if(!p.success)redirect("/recuperar-contrasena?error=validation");const s=await createClient();await s.auth.resetPasswordForEmail(p.data.email.toLowerCase(),{redirectTo:`${process.env.NEXT_PUBLIC_APP_URL}/actualizar-contrasena`});redirect("/recuperar-contrasena?sent=1")}