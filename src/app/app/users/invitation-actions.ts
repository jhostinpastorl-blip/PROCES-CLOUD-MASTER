"use server";import{z}from"zod";import{revalidatePath}from"next/cache";import{createClient}from"@/lib/supabase/server";import{requirePermission}from"@/lib/auth/permissions";import{audit}from"@/lib/audit/log";
export async function revokeInvitation(f:FormData){const companyId=z.string().uuid().parse(f.get("companyId"));const id=z.string().uuid().parse(f.get("id"));await requirePermission(companyId,"users.invite");const s=await createClient();const{data,error}=await s.from("company_invitations").update({status:"revoked"}).eq("id",id).eq("company_id",companyId).eq("status","pending").select("id").maybeSingle();if(error)throw error;if(!data)throw new Error("INVITATION_NOT_REVOCABLE");await audit(companyId,"invitation.revoked","company_invitation",id);revalidatePath("/app/users")}
