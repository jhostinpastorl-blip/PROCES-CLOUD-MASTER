import{getActiveCompany}from"@/lib/context/active";import{createClient}from"@/lib/supabase/server";
export default async function Storage(){const c=await getActiveCompany();if(!c)return <main className="app-content"><div className="empty-state"><h3>Selecciona una empresa</h3></div></main>;if(!c.permissions.includes("storage.read"))return <main className="app-content"><div className="empty-state"><h3>Sin acceso</h3></div></main>;const s=await createClient();const{data}=await s.from("storage_objects").select("id,original_name,mime_type,size_bytes,provider,created_at").eq("company_id",c.companyId).is("deleted_at",null).order("created_at",{ascending:false}).limit(100);return <main className="app-content"><div className="page-head"><div><span className="eyebrow">DOCUMENTOS</span><h1>Archivos</h1><p className="sub">Metadatos centralizados; proveedor desacoplado.</p></div></div><section className="panel"><div className="table-like">{data?.map(x=><div className="table-row" key={x.id}><div><b>{x.original_name}</b><small>{x.mime_type} · {x.provider}</small></div><span>{Math.round((x.size_bytes??0)/1024)} KB</span></div>)}</div>{!data?.length&&<div className="empty-inline">Aún no hay archivos registrados.</div>}</section></main>}