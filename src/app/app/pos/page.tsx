import{getResolvedContext}from"@/lib/company/resolve";import{requireModule}from"@/lib/modules/entitlements";export default async function Pos(){const c=await getResolvedContext();if(!c)return <main className="container section"><h1>POS</h1><p>Selecciona empresa.</p></main>;await requireModule(c.company.companyId,"pos");return <main className="container section"><b>FASE 1 · PREPARACIÓN</b><h1 style={{fontSize:42}}>POS Cloud</h1><p className="sub">El módulo está conectado al Core y a los entitlements, pero su dominio comercial todavía no se inicia hasta aprobar RC0.</p></main>}