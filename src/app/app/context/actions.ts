"use server";import{z}from"zod";import{redirect}from"next/navigation";import{setActiveContext as persistActiveContext}from"@/lib/context/active";export async function selectContext(f:FormData){const companyId=z.string().uuid().parse(f.get("companyId"));const branch=String(f.get("branchId")||"");await persistActiveContext(companyId,branch||undefined);redirect("/app/dashboard")}export async function setActiveContext(f:FormData){return selectContext(f)}