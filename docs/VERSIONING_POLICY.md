# Versionado
Desde v0.34:
- `package.json` y `src/lib/releases/version.ts` deben coincidir;
- documentación de estado usa la misma versión;
- no avanzar release sin actualizar ambos.

La divergencia histórica 0.15.0 vs release interno queda corregida desde v0.34.
