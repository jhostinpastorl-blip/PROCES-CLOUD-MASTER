name: Core CI
on:
  pull_request:
  push:
    branches: [main]

jobs:
  database-security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Start isolated Supabase and apply the complete migration chain
        run: supabase start
      - name: Run pgTAP security and tenant-isolation tests
        run: supabase test db

  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: bash scripts/rc0_static_suite.sh
      - run: node scripts/check_lockfile.mjs

  build:
    needs: [static, database-security]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
