"use server";import{z}from"zod";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{requireCompany}from"@/lib/auth/context";import{saveOnboardingState}from"@/lib/onboarding/persist";
export async function selectModules(f:FormData){const companyId=z.string().uuid().parse(f.get("companyId"));await requireCompany(companyId);const modules=f.getAll("modules").map(String);const s=await createClient();const{error}=await s.rpc("set_initial_company_modules",{p_company_id:companyId,p_codes:modules});if(error)throw error;await saveOnboardingState("branch",{companyId,modules});redirect(`/onboarding/branch?company=${companyId}`)}
