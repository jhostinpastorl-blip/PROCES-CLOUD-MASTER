"use server";import{z}from"zod";import{revalidatePath}from"next/cache";import{createClient}from"@/lib/supabase/server";import{requirePermission}from"@/lib/auth/permissions";import{audit}from"@/lib/audit/log";import{assertBranchLimit}from"@/lib/plans/limits";
const schema=z.object({companyId:z.string().uuid(),name:z.string().min(2).max(120),code:z.string().min(1).max(30).regex(/^[A-Za-z0-9_-]+$/)});
export async function createBranch(f:FormData){const p=schema.parse(Object.fromEntries(f));await requirePermission(p.companyId,"branches.manage");await assertBranchLimit(p.companyId);const s=await createClient();const{data,error}=await s.from("branches").insert({company_id:p.companyId,name:p.name,code:p.code.toUpperCase(),is_active:true}).select("id").single();if(error)throw error;await audit(p.companyId,"branch.created","branch",data.id,{name:p.name,code:p.code});revalidatePath("/app/branches")}
