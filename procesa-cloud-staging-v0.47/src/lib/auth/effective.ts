import{createClient}from"@/lib/supabase/server";export async function assertActiveMembership(companyId:string){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)throw new Error("UNAUTHENTICATED");const{data}=await s.from("company_memberships").select("id,status").eq("company_id",companyId).eq("user_id",user.id).eq("status","active").maybeSingle();if(!data)throw new Error("NO_ACTIVE_MEMBERSHIP");return data}