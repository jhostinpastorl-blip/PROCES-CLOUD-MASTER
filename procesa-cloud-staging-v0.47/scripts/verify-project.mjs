import fs from"node:fs";import path from"node:path";const root=process.cwd();const required=["package.json",".env.example","next.config.ts","src/app","supabase/migrations",".github/workflows/ci.yml","docs/QA_ACCEPTANCE_CRITERIA.md"];let failed=false;for(const p of required){if(!fs.existsSync(path.join(root,p))){console.error("MISSING",p);failed=true}}const env=fs.readFileSync(path.join(root,".env.example"),"utf8");for(const bad of["service_role=","password=","private_key="])if(env.toLowerCase().includes(bad)){console.error("POTENTIAL_SECRET",bad);failed=true}const migrations=fs.readdirSync(path.join(root,"supabase/migrations")).filter(x=>x.endsWith(".sql")).sort();const prefixes=migrations.map(x=>x.split("_")[0]);if(new Set(prefixes).size!==prefixes.length){console.error("DUPLICATE_MIGRATION_PREFIX");failed=true}console.log(`Verified ${migrations.length} migrations and project structure.`);process.exit(failed?1:0);