-- QA ONLY. Never execute in production.
-- Create test auth users through Supabase Auth, then bind their UUIDs manually.
-- Expected tenants: QA Company A / QA Company B.
-- This file intentionally contains no real credentials or secrets.
select 'Use Supabase QA project only' as warning;
